# Sample captures

Place `.pcap` and `.pcapng` demo files in this directory.

These captures will be used in the "File di esempio" section so beginners can
try the analyzer without recording their own traffic.
